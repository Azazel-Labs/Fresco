# CPU reference algorithms

These geometry and particle-allocation algorithms provide independent CPU test
references. They do not render, allocate GPU resources, or ship in the playground.
The TypeScript engine renderers and GPU helpers have been deleted. All GPU specs
run against the shared Rust example engine.
